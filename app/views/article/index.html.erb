<h1>All Articles</h1>

<% Article.all.each do |article| %>
	<h3><%= article.title %></h3>
	<%= article.content %>
	<br><br>
	<% @user_id = article.user_id %>
	<b>-<%= User.find(@user_id).fname %>
	<%= User.find(@user_id).lname %></b>
	<br>
	<br>
<% end %>

<%= link_to "All Users", user_path %>